# Python
__pycache__/
*.pyc
*.pyo

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Local-only
.claude/
CLAUDE.md

# Runtime colour mode selection — per-device, not part of the library
mode.cfg

# Token source JSON — design tooling inputs, not needed by consumers
tokens/src/$metadata.json
tokens/src/$themes.json
tokens/src/01-primitive/
tokens/src/02-Semantic/
tokens/src/03-viewports/

# Icon source PNGs — design tooling inputs, not needed by consumers
icons/src/*.png

# Environment & secrets
.env
.env.*
*.key
*.pem

# Editor
.vscode/
.idea/
